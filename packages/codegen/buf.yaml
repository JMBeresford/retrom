version: v2
modules:
  - path: protos
deps:
  - buf.build/googleapis/googleapis
lint:
  use:
    - COMMENTS
    - STANDARD
  except:
    - RPC_REQUEST_RESPONSE_UNIQUE
    - RPC_RESPONSE_STANDARD_NAME
  ignore:
    - protos/igdbapi.proto
    - protos/google
    - protos/retrom/services/saves/v1/saves-service.proto
    - protos/retrom/services/saves/v1/models.proto
breaking:
  use:
    - FILE
